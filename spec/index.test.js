describe('index', () => {
  it('should be true', () => {
    const testVar = true;
    expect(testVar).toBe(true)
  });

  it('should be false', () => {
    const testVar = true;
    expect(testVar).toBe(false)
  });
})